Git ignores the `./config/config.json` file. To use this software, you must first create the configuration for Sequelize:

    $ sequelize init:config;

And populate the file with your DBMS configuration
